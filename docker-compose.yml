services:
  frontend:
    container_name: react
    build: ./frontend/
    networks:
      - client-side
    ports:
      - 3000:3000
    volumes:
      - ./frontend:/code
  backend:
    container_name: nest
    build: ./backend/
    networks:
      - client-side
    ports:
      - 4000:4000
    volumes:
      - ./backend:/code
      #    depends_on:
      #- backend
        #    env_file:
        #      - .env
        #  backend:
        #    container_name: nestjs
        #    build: ./backend/
        #    depends_on:
        #      - database
        #    env_file:
        #      - .env
        #  db:
        #    image: postgres:15.4-alpine
        #    env_file:
        #      - .env
networks:
  client-side: {}
